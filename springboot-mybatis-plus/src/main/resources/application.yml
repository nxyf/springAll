server:
  port: 8080
  servlet:
    context-path: /
spring:
  application:
    name: mybatisApplication
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.253.128:3306/mybatis
    username: root
    password: root
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
    serialization:
      write-dates-as-timestamps: false
mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
logging:
  level:
   com.nxyf: debug
